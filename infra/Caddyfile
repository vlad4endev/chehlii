# Публичный вход: IP (проверка) и кейстоп.рф (прод).
# www и apex оба отдают сайт (без редиректа), пока DNS A-записей расходится.

http://:80 {
	reverse_proxy backend:8000
}

кейстоп.рф, www.кейстоп.рф {
	encode gzip
	reverse_proxy backend:8000
}
