# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node
node_modules/
dist/
build/
.vite/
*.local

# Env / secrets
.env
.env.*
!.env.example
!infra/.env.example
*.pem
*.key

# IDE / OS
.idea/
.vscode/
.DS_Store

# Data / logs
*.log
/data/
/tmp/
scratchpad/

# Alembic local
backend/alembic/versions/*.pyc

# Локальное состояние claude-flow (pid'ы, метрики, статистика) — у каждой машины своё
/.claude-flow/

# Локальное состояние плагина ruflo (перезаписывается на каждой машине).
# .claude/launch.json — командный конфиг дев-серверов, он остаётся в репозитории.
/.claude/proven-config.json
/.claude/.proven-config-version
